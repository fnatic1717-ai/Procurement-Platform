import { Injectable } from '@nestjs/common';
import { hasPermission, type AuthenticatedPrincipal } from '@procurement/shared';
export interface AuthorizationRequest { permission: string; tenantId: string; objectOwnerId?: string; actorRole?: string; justification?: string; independentApprovalUserId?: string; }
@Injectable()
export class PolicyService { can(principal: AuthenticatedPrincipal | null, req: AuthorizationRequest): boolean { if (!principal) return false; if (principal.tenantId !== req.tenantId) return false; return hasPermission(principal, req.permission); } enforceRequesterCannotApproveOwn(principal: AuthenticatedPrincipal, requesterId: string, override?: AuthorizationRequest): boolean { if (principal.userId !== requesterId) return true; return Boolean(override?.justification && override.independentApprovalUserId && override.independentApprovalUserId !== principal.userId); } enforceBuyerCannotSolelyApproveOwnAward(principal: AuthenticatedPrincipal, buyerId: string, override?: AuthorizationRequest): boolean { if (principal.userId !== buyerId) return true; return Boolean(override?.justification && override.independentApprovalUserId && override.independentApprovalUserId !== principal.userId); } }
